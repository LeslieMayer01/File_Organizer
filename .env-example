# 📁 Configuration file for File Organizer

# Path to the folder containing files to organize (MUST exist)
FOLDER_TO_ORGANIZE=/absolute/path/to/input/folder

# Path where all reports will be saved (default is ./reports/)
REPORTS_DIR=/app/reports

# Excel template path (used in step 8)
EXCEL_TEMPLATE_PATH=resources/template.xlsm

# Mapping file for C0 subfolders (used in step 7)
FOLDER_MAPPINGS=resources/folder_mappings.json

# Enable or disable simulation for each step (true or false)
# If true: actions will be simulated and logged only (dry run)
# If false: changes will be executed and reported

SIMULATE_STEP_1=true
SIMULATE_STEP_2=true
SIMULATE_STEP_3=true
SIMULATE_STEP_4=true
SIMULATE_STEP_5=true
SIMULATE_STEP_6=true
SIMULATE_STEP_7=true
SIMULATE_STEP_8=true
SIMULATE_STEP_9=true

# Juzgado ID used to match folder prefixes (used in most steps)
JUDGEMENT_ID=053804089001
